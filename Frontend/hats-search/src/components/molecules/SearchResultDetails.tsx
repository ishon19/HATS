import React from "react";
import { ISearchResultDetails } from "../../interfaces/interface";

const SearchResultDetails = (props: ISearchResultDetails) => {
    
};

export default SearchResultDetails;
